#include "soundex.h"
#include "damerau-levenshtein.h"
#include <stdio.h>
#include <stdlib.h>
#include "fuzzy.h"
#include "search-utils.h"
#include "synonyms.h"

int main(){
    char* query = "harry potter";
    int* results = fuzzy_search(query, "dict_soundex.csv");
    int i = 0;
    while(results[i] != -1){
        printf("%d no results index -> %d\n",i,results[i]);;
        ++i;
    }
    return 0;
}