#include "fuzzy.h"
#include <stdio.h>
#include "preprocess.h"

int main(){
    preprocess();
    char* query = "taylor";
    int* fuzz_res = fuzzy_search(query, 0, "dict_soundex.csv");
    for (int i = 0; fuzz_res[i] != -1; ++i){
        printf("%d -> %d\n",i,fuzz_res[i]);
    }
    return 0;
}